# REQUIRED: Deployer private key (with 0x prefix)
# WARNING: Never commit this to git. Add .env to .gitignore
PRIVATE_KEY=your_private_key_here

# REQUIRED: Treasury address (where protocol fees go)
# Use a separate wallet or multisig. DO NOT use deployer address in production
TREASURY_ADDRESS=0xYourTreasuryAddressHere

# OPTIONAL: How much AVAX/ETH to fund the controller (in wei)
# Default is 0.1 ether if not set
FUNDING_AMOUNT=100000000000000000  # 0.1 AVAX

# REQUIRED for running the script: RPC URL
# For Avalanche mainnet:
RPC_URL=https://api.avax.network/ext/bc/C/rpc

# For Avalanche Fuji testnet (recommended for testing):
# RPC_URL=https://api.avax-test.network/ext/bc/C/rpc